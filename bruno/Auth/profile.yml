info:
  name: profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/auth/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
